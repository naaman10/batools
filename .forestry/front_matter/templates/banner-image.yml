---
label: Banner Image
hide_body: true
fields:
- name: images
  type: field_group_list
  fields:
  - name: image
    type: file
    config:
      maxSize: 64
    label: image
  - name: alt_text
    type: text
    config:
      required: false
    label: Alt Text
  config:
    min: 
    max: 
    labelField: 
  label: images
